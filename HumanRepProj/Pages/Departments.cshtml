@page
@model DepartmentsModel
@{
    ViewData["Title"] = "Departments";
    Layout = "_Layout";
}

@section Styles {
    <style>
        .scrollable-container {
            position: absolute; /* Position it properly */
            left: 256px; /* Match sidebar width exactly */
            top: 0;
            width: calc(100% - 256px); /* Take remaining width */
            height: 100vh; /* Full viewport height */
            overflow-y: auto; /* Single scrollbar */
            padding: 20px; /* Content padding */
            box-sizing: border-box;
        }

            /* Scrollbar styling */
            .scrollable-container::-webkit-scrollbar {
                width: 8px;
            }

            .scrollable-container::-webkit-scrollbar-thumb {
                background-color: #0d6efd;
                border-radius: 4px;
            }

            .scrollable-container::-webkit-scrollbar-track {
                background-color: #f8f9fa;
            }

        .department-card {
            transition: transform 0.3s;
            height: 100%;
        }

            .department-card:hover {
                transform: translateY(-5px);
            }

            .department-card .card-body {
                display: flex;
                flex-direction: column;
            }

        .department-actions {
            margin-top: auto;
            display: flex;
            gap: 0.5rem;
        }
    </style>
}

<div class="scrollable-container">
<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
    <h1 class="h2">Department Management</h1>
    <div class="btn-toolbar mb-2 mb-md-0">
        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#addDepartmentModal">
            <i class="bi bi-plus-circle me-1"></i> Add Department
        </button>
    </div>
</div>

<!-- Display error message if exists -->
@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        @TempData["ErrorMessage"]
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
}

<div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4">
    @foreach (var department in Model.Departments)
    {
        <div class="col">
            <div class="card department-card shadow-sm">
                <div class="card-body">
                    <div class="d-flex justify-content-between align-items-start">
                        <div>
                            <h5 class="card-title">@department.Name</h5>
                            <h6 class="card-subtitle mb-2 text-muted">@department.EmployeeCount Employees</h6>
                        </div>
                        <span class="badge @department.StatusClass">@department.Status</span>
                    </div>
                    <p class="card-text mt-2">@department.Description</p>
                    <div class="department-actions">
                        <button class="btn btn-sm btn-outline-primary edit-department"
                                data-id="@department.DepartmentID"
                                data-bs-toggle="modal"
                                data-bs-target="#editDepartmentModal">
                            <i class="bi bi-pencil"></i> Edit
                        </button>
                        <button class="btn btn-sm btn-outline-danger delete-department"
                                data-id="@department.DepartmentID"
                                data-bs-toggle="modal"
                                data-bs-target="#deleteDepartmentModal">
                            <i class="bi bi-trash"></i> Delete
                        </button>
                    </div>
                </div>
            </div>
        </div>
    }
</div>

<!-- Add Department Modal -->
<div class="modal fade" id="addDepartmentModal" tabindex="-1" aria-labelledby="addDepartmentModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" asp-page-handler="Create">
                <div class="modal-header">
                    <h5 class="modal-title" id="addDepartmentModalLabel">Add New Department</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label asp-for="Input.Name" class="form-label">Department Name</label>
                        <input asp-for="Input.Name" class="form-control" required>
                        <span asp-validation-for="Input.Name" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Input.Description" class="form-label">Description</label>
                        <textarea asp-for="Input.Description" class="form-control" rows="3"></textarea>
                        <span asp-validation-for="Input.Description" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Input.Performance" class="form-label">Performance</label>
                        <input asp-for="Input.Performance" class="form-control" required>
                        <span asp-validation-for="Input.Performance" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Input.Budget" class="form-label">Budget</label>
                        <input asp-for="Input.Budget" class="form-control" required>
                        <span asp-validation-for="Input.Budget" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Input.Status" class="form-label">Status</label>
                        <select asp-for="Input.Status" class="form-control" required>
                            <option value="Active">Active</option>
                            <option value="Inactive">Inactive</option>
                        </select>
                        <span asp-validation-for="Input.Status" class="text-danger"></span>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-primary">Save Department</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Edit Department Modal -->
<div class="modal fade" id="editDepartmentModal" tabindex="-1" aria-labelledby="editDepartmentModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" asp-page-handler="Edit">
                <input type="hidden" asp-for="Input.DepartmentID" id="editDepartmentId" />
                <div class="modal-header">
                    <h5 class="modal-title" id="editDepartmentModalLabel">Edit Department</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="mb-3">
                        <label asp-for="Input.Name" class="form-label">Department Name</label>
                        <input asp-for="Input.Name" class="form-control" id="editDepartmentName" required>
                        <span asp-validation-for="Input.Name" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Input.Description" class="form-label">Description</label>
                        <textarea asp-for="Input.Description" class="form-control" id="editDepartmentDescription" rows="3"></textarea>
                        <span asp-validation-for="Input.Description" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Input.Performance" class="form-label">Performance</label>
                        <input asp-for="Input.Performance" class="form-control" id="editDepartmentPerformance" required>
                        <span asp-validation-for="Input.Performance" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Input.Budget" class="form-label">Budget</label>
                        <input asp-for="Input.Budget" class="form-control" id="editDepartmentBudget" required>
                        <span asp-validation-for="Input.Budget" class="text-danger"></span>
                    </div>
                    <div class="mb-3">
                        <label asp-for="Input.Status" class="form-label">Status</label>
                        <select asp-for="Input.Status" class="form-control" id="editDepartmentStatus" required>
                            <option value="Active">Active</option>
                            <option value="Inactive">Inactive</option>
                        </select>
                        <span asp-validation-for="Input.Status" class="text-danger"></span>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-primary">Save Changes</button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Delete Department Modal -->
<div class="modal fade" id="deleteDepartmentModal" tabindex="-1" aria-labelledby="deleteDepartmentModalLabel" aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <form method="post" asp-page-handler="Delete">
                <input type="hidden" id="deleteDepartmentId" name="id" />
                <div class="modal-header">
                    <h5 class="modal-title" id="deleteDepartmentModalLabel">Confirm Deletion</h5>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <p>Are you sure you want to delete this department? This action cannot be undone.</p>
                    <div class="alert alert-warning" role="alert">
                        <i class="bi bi-exclamation-triangle-fill me-2"></i>Deleting this department will reassign all employees to "Unassigned".
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    <button type="submit" class="btn btn-danger">Delete Department</button>
                </div>
            </form>
        </div>
    </div>
</div>
</div> 

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script>
        // Initialize tooltips
        var tooltipTriggerList = [].slice.call(document.querySelectorAll('[data-bs-toggle="tooltip"]'))
        var tooltipList = tooltipTriggerList.map(function (tooltipTriggerEl) {
            return new bootstrap.Tooltip(tooltipTriggerEl)
        });

        // Department search functionality
        document.getElementById('departmentSearch')?.addEventListener('input', function() {
            const searchValue = this.value.toLowerCase();
            const departments = document.querySelectorAll('.department-card');

            departments.forEach(department => {
                const title = department.querySelector('.card-title').textContent.toLowerCase();
                const description = department.querySelector('.card-text').textContent.toLowerCase();

                if (title.includes(searchValue) || description.includes(searchValue)) {
                    department.parentElement.style.display = 'block';
                } else {
                    department.parentElement.style.display = 'none';
                }
            });
        });

        // Edit department modal setup
        document.querySelectorAll('.edit-department').forEach(button => {
            button.addEventListener('click', async function() {
                const departmentId = this.getAttribute('data-id');

                try {
                    const response = await fetch(`?handler=DepartmentDetails&id=${departmentId}`);
                    const data = await response.json();

                    if (data.success) {
                        document.getElementById('editDepartmentId').value = data.department.DepartmentID;
                        document.getElementById('editDepartmentName').value = data.department.Name;
                        document.getElementById('editDepartmentDescription').value = data.department.Description;
                        document.getElementById('editDepartmentPerformance').value = data.department.Performance;
                        document.getElementById('editDepartmentBudget').value = data.department.Budget;
                        document.getElementById('editDepartmentStatus').value = data.department.Status;
                    } else {
                        alert(data.message);
                    }
                } catch (error) {
                    console.error('Error fetching department details:', error);
                    alert('Error loading department details');
                }
            });
        });

        // Delete department modal setup
        document.querySelectorAll('.delete-department').forEach(button => {
            button.addEventListener('click', function() {
                const departmentId = this.getAttribute('data-id');
                document.getElementById('deleteDepartmentId').value = departmentId;
            });
        });
    </script>
}